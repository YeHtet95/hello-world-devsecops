# Included into the http{} block by the base image's conf.d glob.
#
# Suppress the nginx version in Server: headers and error pages. Version
# disclosure is not a vulnerability in itself, but it hands an attacker a
# free CVE lookup against the exact build. 
server_tokens off;
